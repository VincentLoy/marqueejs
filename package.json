{
  "name": "marqueejs",
  "version": "0.0.1",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "vite docs/src --open",
    "build": "vite build",
    "build:docs": "vite build docs/src --outDir ../dist",
    "preview": "bun run build:docs && vite docs/dist --open",
    "test": "bun test"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "tailwindcss": "^3.0.0",
    "postcss": "^8.0.0",
    "autoprefixer": "^10.0.0",
    "@types/bun": "latest",
    "serve": "^14.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}