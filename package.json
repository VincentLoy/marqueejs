{
  "name": "marqueejs",
  "version": "0.1.0",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "vite docs/src --open",
    "build": "vite build",
    "build:docs": "vite build docs/src --outDir ../dist --emptyOutDir",
    "preview": "bun run build:docs && vite docs/dist --open",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22.10.5",
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0",
    "serve": "^14.0.0",
    "tailwindcss": "^3.0.0",
    "vite": "^5.0.0"
  },
  "peerDependencies": {
    "typescript": "^5.7.2"
  }
}